export type MovieType = {
    id: number,
    title: string,
    description: string,
    duration: string,
    director: string,
    access_level: string,
    price: number,
    banner_url: string,
    title_url: string
}